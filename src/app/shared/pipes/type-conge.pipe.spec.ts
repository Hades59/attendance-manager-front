import { TypeCongePipe } from './type-conge.pipe';

describe('TypeCongePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeCongePipe();
    expect(pipe).toBeTruthy();
  });
});
